console.log( 'util lulz!' );

// second way
export const subtract = ( a, b ) => a - b;

// first way..down later
const square = (x) => {
  return x * x;
}

const add = (a, b) => a + b;

// first way, here
export { square, add };
